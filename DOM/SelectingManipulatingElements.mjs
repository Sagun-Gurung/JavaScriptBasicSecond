// selecting elements  using method like getElementById, getElementByClassName, getElementByTagName

let element = getElementById("header");

// Manipulating Elements
// You can manipulate elements by adding or removing content, styles, and event listeners.

element.style.color = "red";

element.addEventListener("click", function () {
  console.log("Header Clicked");
});
